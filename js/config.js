/**
 * Ledger — one-time site configuration.
 *
 * Paste your deployed Apps Script Web App URL between the quotes below.
 * Once it's here and pushed to GitHub, ANY device that opens the site link
 * is connected straight away — nobody has to paste anything, and uploaded
 * files show up immediately on every phone, laptop and browser.
 *
 * Get the URL from: Apps Script → Deploy → Manage deployments → the
 * "Web app" URL. It ends in /exec, like:
 *   https://script.google.com/macros/s/AKfycbXXXXXXXXXXXX/exec
 *
 * Leave it as "" only if you want each person to connect manually on the
 * Data tab instead (the old behaviour).
 *
 * Note: anyone with your site link can then read and write that data, the
 * same as sharing a Drive folder link. Keep the repo private, or keep the
 * site URL to your team.
 */
export const APPS_SCRIPT_URL = "";
